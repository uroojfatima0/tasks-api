<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Task</title>
    <style>
        /* Background with Gradient */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #fdfbfb, #e3eaf4);
            text-align: center;
            overflow: hidden;
        }

        /* Glowing Form Container */
        .form-container {
            background: white;
            padding: 35px;
            border-radius: 12px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            gap: 20px;
            width: 380px;
            position: relative;
            animation: glow 4s infinite alternate;
        }

        @keyframes glow {
            0% { box-shadow: 0px 0px 20px rgba(255, 64, 129, 0.3); }
            100% { box-shadow: 0px 0px 40px rgba(255, 64, 129, 0.6); }
        }

        h1 {
            font-size: 2.4rem;
            color: #ff4081;
            text-shadow: 2px 2px 10px rgba(255, 64, 129, 0.3);
            margin-bottom: 20px;
            animation: fadeIn 2s ease-in-out;
        }

        /* Input Fields */
        input, textarea {
            width: 100%;
            padding: 14px;
            margin-bottom: 10px;
            font-size: 1rem;
            border: 2px solid #ff4081;
            border-radius: 8px;
            outline: none;
            transition: all 0.3s ease-in-out;
            background: #fff5f8;
            box-sizing: border-box;
        }

        textarea {
            height: 52px;
            min-height: 52px !important; /* Same as input height */
            max-height: 250px;
            resize: vertical;
        }

        input:focus, textarea:focus {
            border-color: #e91e63;
            box-shadow: 0px 0px 10px rgba(233, 30, 99, 0.4);
        }

        /* Submit Button */
        button {
            font-size: 1.2rem;
            padding: 12px;
            color: white;
            background: linear-gradient(135deg, #ff4081, #ff79a9);
            border: none;
            border-radius: 50px;
            box-shadow: 0px 10px 20px rgba(255, 64, 129, 0.4);
            transition: all 0.3s ease-in-out;
            cursor: pointer;
            animation: pulse 2s infinite;
            width: 100%;
        }

        button:hover {
            background: linear-gradient(135deg, #e91e63, #ff5a8e);
            box-shadow: 0px 10px 25px rgba(233, 30, 99, 0.5);
            transform: scale(1.05);
        }

        a {
            margin-top: 15px;
            font-size: 1.2rem;
            color: #ff4081;
            text-decoration: none;
            transition: all 0.3s ease-in-out;
        }

        a:hover {
            color: #e91e63;
            text-decoration: underline;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

    </style>
</head>
<body>
    <h1>🚀 Create New Task</h1>
    <div class="form-container">
        <form action="/tasks/create" method="POST">
            <input type="text" name="title" placeholder="Task Title" required>
            <textarea name="description" placeholder="Task Description" required></textarea>
            <button type="submit">✨ Create Task</button>
        </form>
    </div>
    <a href="/tasks/list">⬅️ Back to Tasks</a>
</body>
</html>
