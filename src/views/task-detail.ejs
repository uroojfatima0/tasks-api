<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <style>
        /* General Styles */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #fdfbfb, #e3eaf4);
            text-align: center;
            padding: 50px;
        }

        /* Task Container */
        .task-container {
            background: white;
            max-width: 500px;
            margin: 0 auto;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.1);
        }

        /* Task Title - Only this will animate from top */
        h1 {
            font-size: 2rem;
            color: #ff4081;
            text-shadow: 2px 2px 10px rgba(255, 64, 129, 0.3);
            margin-bottom: 10px;
            animation: slideFromTop 1s ease-in-out;
        }

        /* Task Description */
        p {
            font-size: 1.2rem;
            color: #333;
            background: rgba(255, 64, 129, 0.1);
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0px 5px 10px rgba(255, 64, 129, 0.1);
        }

        /* Back Button */
        .back-link {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 20px;
            font-size: 1.1rem;
            text-decoration: none;
            font-weight: bold;
            color: white;
            background: linear-gradient(135deg, #ff4081, #ff79a9);
            border-radius: 50px;
            transition: all 0.3s ease-in-out;
            box-shadow: 0px 5px 15px rgba(255, 64, 129, 0.4);
        }

        .back-link:hover {
            background: linear-gradient(135deg, #e91e63, #ff5a8e);
            box-shadow: 0px 10px 25px rgba(233, 30, 99, 0.5);
            transform: scale(1.05);
        }

        /* Title Animation - Slide from Top */
        @keyframes slideFromTop {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <div class="task-container">
        <h1>📌 Task: <%= task.title.charAt(0).toUpperCase() + task.title.slice(1) %></h1>
        <p><%= task.description.charAt(0).toUpperCase() + task.description.slice(1) %></p>
        <a href="/tasks/list" class="back-link">⬅️ Back to Tasks</a>
    </div>
</body>
</html>
